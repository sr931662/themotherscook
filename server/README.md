# Server

Empty on purpose. The backend for The Mothers Cook (enquiry form submission, quote requests, etc.) will be built here later.

The client (`../client`) currently has no server dependency — see the `TODO` markers in `client/src/data/site.ts` and the section components for where an enquiry API will plug in.
